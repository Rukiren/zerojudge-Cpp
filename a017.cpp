#include <bits/stdc++.h>
using namespace std;

int main()
{
    while (1)
    {
        string a;
        cin >> a;
        int x = getline(a);
        int ans = 0;
        for (int i = 0; i < x; i++)
        {
        }
    }
}